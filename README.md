# Spam-Email-Detection

This project builds a spam detection model using Scikit-learn and a dataset of SMS messages. It processes the text data, trains a Naive Bayes classifier to distinguish between spam and non-spam (ham) messages, and evaluates the model's accuracy and performance.
